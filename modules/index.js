/**
 * ============================================
 * MODULES - Barrel Exports
 * Centralizando exports dos módulos principais
 * ============================================
 */

const WhatsAppBot = require('./whatsapp');
const FlowEngine = require('./flow-engine');

module.exports = {
    WhatsAppBot,
    FlowEngine
};
